-- name: test_iceberg_equality_delete_null_key
-- Iceberg spec: a NULL in an equality-delete column matches NULL row values, so both NULL-id rows must be deleted.
create external catalog iceberg_eqnull_${uuid0} PROPERTIES ("type"="iceberg", "iceberg.catalog.type"="hadoop", "iceberg.catalog.warehouse"="oss://${oss_bucket}/iceberg_ci_fixtures/eq_delete_null_key", "aws.s3.access_key"="${oss_ak}", "aws.s3.secret_key"="${oss_sk}", "aws.s3.endpoint"="${oss_endpoint}");

set new_planner_optimize_timeout = 30000;

select * from iceberg_eqnull_${uuid0}.eq_delete_db.eq_del_convert order by id;

drop catalog iceberg_eqnull_${uuid0};
